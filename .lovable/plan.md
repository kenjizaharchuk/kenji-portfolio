## Match Spiber detail title to card

One-line data change in `src/data/projects.ts`: update the Spiber project's `title` from `'Spiber'` to `'Spiber Brewed Protein'` so the detail view matches the carousel card and the morph animation lands on identical text.

No other changes. Carousel, MorphLayer, and routing untouched.
